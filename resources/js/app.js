import './bootstrap';
document.getElementById('add-picture-btn').addEventListener('click', function() {
    // Logic to add a new input field for picture upload
});

// Example: Remove picture dynamically
document.querySelectorAll('.remove-picture-btn').forEach(function(btn) {
    btn.addEventListener('click', function() {
        // Logic to remove the corresponding picture input field
    });
});